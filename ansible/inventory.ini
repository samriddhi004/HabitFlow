[app_servers]
# For local deployment (same machine as Jenkins):
localhost ansible_connection=local

# For a remote server, replace with:
# 192.168.x.x ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa
